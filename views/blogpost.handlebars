<div>
    <h1>{{title}}</h1>
    <p>by <a href="/author/{{info.user_id}}">{{name}}</a> </p>
    {{{blog}}}

    {{#if editor}}<button
            onclick="window.location.href='/edit/post/{{id}}'"
        >Edit</button>{{/if}}
    {{#if editor}}
        <button
            onclick="window.location.href='/delete/post/{{id}}'"
        >Delete</button>
    {{/if}}
</div>